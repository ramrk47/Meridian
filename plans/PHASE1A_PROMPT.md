# Builder prompt — Phase 1a (exam-agnostic data model + DocTutorials)

> Paste into a fresh session. You are the **builder**; the coordinator owns strategy/PROGRESS.
> Execute **Phase 1a only** (data layer). Do **not** touch mobile/CSS — that's Phase 1b.
> Vanilla local-first web app: `app.js` / `styles.css` / `index.html` + `data.js` generated by `build_data.py`.

## Orient (read first)
- `plans/PROGRESS.md` and the **Sequenced roadmap** in `plans/VISION_and_ROADMAP.md`.
- `plans/DATA_INTEGRATION.md` (target schema, CANON aliases, build steps, sanity numbers).
- Skim in `app.js`: the data-access helpers (`subjectsOf`, `leavesOf`, `treeOf`, `CANON`, `scoredCross`/`crossMatches`, palette + drawer) and how they read `window.QBANK_DATA`.
- Skim `build_data.py` and the DocTutorials capture in `_raw/NewPlatforms/`.

## Task (data only)
1. **Refactor the model** so `build_data.py` emits:
   `window.D = { exam:"NEET PG / INI-CET", platforms:[ {id,name,kind:"qbank",subjects:[ {subject, modules:[{id,category,name,mcqs,rating,priority,hyScore}]} ]} ], tests:[...], videos:[...] }`.
   Keep stable `id`s. Keep a **back-compat shim** (`window.QBANK_DATA` alias) during the transition to limit churn; remove it only once all reads point at `D.platforms`.
2. **Integrate DocTutorials** (it has chapter + MCQ totals). **Do NOT** add PrepLadder/eGurukul yet (counts not drilled) — leave a clean ingest seam for them.
3. **Extend `CANON`** with the new aliases (`OB & G`/`OBG`→Obstetrics & Gynaecology, `PSM`→Community Medicine, etc., per DATA_INTEGRATION.md). All platforms share the 19-subject spine.
4. **Consensus 2 → N**: "high on ≥2 independent platforms." Cross-platform matcher, drawer "seen on other platform," and palette iterate `D.platforms` — no hardcoded `marrow`/`cerebellum` keys.
5. **QBank platform toggle → N-way switch** (segmented; dropdown when >3). Subject sidebar + tree unchanged.

## Hard constraints
- **Preserve totals:** Marrow 21,915 + Cerebellum 20,974 = **42,889** MCQs must not change. Compute DocTutorials total = sum of captured per-subject totals; **report the number**.
- **Never fabricate counts.** Where MCQ counts are missing, fall back to a neutral tier — do not fake `hyScore` from absent data. Label inferred data.
- Stay out of `styles.css` and layout/mobile work.

## Verify → ship
- `python build_data.py`, open in **Claude Preview**: console clean; every tab renders; QBank N-way switch works; consensus/high-yield totals sane; drawer + palette work. Eyeball Overview, QBank, High-Yield.
- Commit in small units; `git push origin main` (SSH; gh token invalid).
- Tick **Phase 1a** in `PROGRESS.md`, append one decision-log line, refresh the data inventory.
- **Report to coordinator (3–6 lines):** what changed, DocTutorials total, any drift, what's ready for 1b.

## Model / reasoning
**Opus 4.8, high reasoning** — data-shape refactor with a back-compat shim and correctness-critical totals; logic-dense, low UI iteration.
