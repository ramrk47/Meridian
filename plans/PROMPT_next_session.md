# Next-session kickoff prompt — Meridian

Paste this as your first message in the next Claude Code chat (run it from `~/Meridian`).

---

You are continuing **Meridian**, a local-first, offline web app that is a cross-platform
tracking + review hub for medical PG exam prep ("IMDB of medical exams"). The repo is at
`~/Meridian` (git, synced to `git@github.com:ramrk47/Meridian.git`, branch `main`). Read these
first, in order:

1. `README.md` — what the app is and how it's built.
2. `plans/VISION_and_ROADMAP.md` — the core concept, vision, and prioritized roadmap.
3. `plans/MARKET_RESEARCH_PLAN.md` — the research you'll run this session.
4. `plans/RESPONSIVE_MOBILE_REWORK.md` — the responsive + mobile rebuild plan.
5. `plans/DATA_INTEGRATION.md` — pending data work (3 new platforms already captured in `_raw/NewPlatforms/`).

**Architecture reminders before you touch code:**
- Static `index.html` + `styles.css` + `app.js` + `data.js` (generated by `build_data.py`) + `storage.js` (localStorage adapter, server-ready).
- Design = warm editorial "almanac" (paper/ink, serif display, muted clinical colors). Do NOT make it a neon dashboard.
- Verify every change in the Claude Preview server and check console for errors before committing.
- Commit in small logical units and `git push origin main` when a milestone is verified.

**This session's mission, in order:**

### Phase 1 — Deep market research (do this first; it's the point of this session)
Run the research in `plans/MARKET_RESEARCH_PLAN.md`: gather **real pain points, feature
requests, and complaints** from NEET PG / INI-CET / FMGE aspirants about Marrow, PrepLadder,
Cerebellum, DocTutorials, eGurukul, DAMS, etc. — from Reddit, Instagram, and (if reachable)
Telegram/YouTube comments. Use the `deep-research` skill and/or WebSearch/WebFetch. Produce
`plans/RESEARCH_FINDINGS.md`: a cited, deduplicated, ranked list of (a) pain points, (b)
requested features, (c) what people love — each tagged to a Meridian feature or a new idea.

### Phase 2 — Synthesize into roadmap deltas
Update `plans/VISION_and_ROADMAP.md` with what the research changes: re-rank the backlog,
add/kill ideas, and write a crisp "next 5 features" list with acceptance criteria.

### Phase 3 — Build (pick the highest-leverage items the research validates)
Likely top of the list (confirm against findings first):
1. **Responsive + mobile rebuild** (`plans/RESPONSIVE_MOBILE_REWORK.md`) — the current desktop
   space utilization is poor and there is no real mobile view. This is foundational.
2. **Multi-platform data model** so DocTutorials/PrepLadder/eGurukul slot in beside Marrow/Cerebellum
   (`plans/DATA_INTEGRATION.md`).
3. Whatever 1–2 research-validated features rank highest.

Work iteratively: build → verify in preview at desktop + tablet + mobile widths → commit → push.

Ask me to confirm the "next 5 features" list before you start large builds.
