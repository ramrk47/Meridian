# Builder prompt — Phase 1c.1F (faculty layer: schema + curated seed)

> Paste into a fresh session. You are the **builder**; the coordinator owns strategy/PROGRESS.
> Add Meridian's **people pillar**: faculty as first-class entities with career history + neutral
> ratings. **Schema + a small curated seed only** — full coverage comes from a later data-gathering pass.
> Run this **after** 1c.1 (it reuses 1c.1's `D.sources[]` registry + epistemic-label conventions).

## Orient (read first — non-negotiable)
- **`plans/FACULTY_LAYER.md`** — THE architecture (entity schema, two rating signals, guardrails, sequencing).
- **`plans/DATA_VISUAL_STANDARD.md`** — neutrality firewall + epistemic labels (`directional` etc.).
- `plans/RESEARCH_FINDINGS.md` (H3 faculty/subject signals) + `MARKET_INTEL.md` — sources you may curate from.
- `build_data.py`, the `D` shape (incl. 1c.1's `D.sources[]`), `D.videos`, and how `app.js` reads `D`.

## Do
1. **Faculty entity schema** — implement `D.faculty[]` exactly per `FACULTY_LAYER.md` (id, name, aka,
   subjects, **affiliations[]** career timeline with `from/to/status: current|past|solo|superspecialty`,
   `ratings.profile` + `ratings.videoByPlatform`, `sourceIds`, `epistemic`). Generated by `build_data.py`
   from a curated source file (`_raw/curated/faculty_seed.*`).
2. **Curated seed (~10–20 faculty)** — only well-known names you can source from the research docs /
   widely-public reputation; fill affiliations + subjects + a `directional` profile rating. **If you
   can't source an affiliation or date, omit it** — leave the field empty, do not guess.
3. **Link to existing data** — map faculty → `D.platforms` (platformId) + subjects (CANON); where a
   CoreBTR video clearly maps to a seeded faculty, set `facultyId` so `videoByPlatform` can roll up
   (skip if uncertain — don't fabricate the mapping).
4. **`verifiedVia` design** — every `ratings.profile` carries `verifiedVia:"in-app-activity"` +
   `status:"directional"`. **Do NOT build voting** (needs backend) — just the field + a seam.
5. **Faculty surface (light)** — a faculty list + a profile view: name, subjects, the **affiliation
   timeline** (current → past → solo/super-specialty), and the directional rating. Every figure shows
   its epistemic label + source. Rich data-viz (timeline/strength charts) is the later 1c.2 pass — keep
   this clean and labeled, not fancy.

## Hard constraints (neutrality — auto-fail if broken)
- **Never fabricate** a name, affiliation, date, or score. Unsourced ⇒ omit.
- **Aggregate-only, community-sentiment framing.** No "worst faculty" ranking; no per-individual shaming.
- Seed ratings are `directional`, labeled + sourced. Preserve all existing `measured` counts (56,091).
- Don't regress the mobile density / no-scroll / console-clean bars.

## Verify → ship
- `python build_data.py`; open in Claude Preview (free port if 87xx taken). Console clean; faculty list +
  profile render; affiliation timeline shows status transitions; every rating shows label + source.
- **Spot-check 3 seeded faculty facts** against the research docs / clearly-public reputation — all must trace.
- Commit small units; `git push origin main` (SSH; gh token invalid).
- Tick **1c.1F** in `PROGRESS.md`, append one decision-log line, refresh the data inventory.
- **Report to coordinator (3–6 lines):** faculty seeded (count), `D.faculty[]` shape, what you omitted
  for lack of a source, how the timeline + labels render, what the data pass should prioritize next.

## Model / reasoning
**Opus 4.8, high reasoning** — neutrality-critical curation of *named individuals*; sourcing discipline
and the schema-for-the-future both matter. Spend the stronger model.
